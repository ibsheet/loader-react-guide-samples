import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  divStyle: {
    paddingBottom: '10px'
  },
  btnStyle: {
    height: '30px',
    background: '#4c4c57',
    color: '#fff',
    marginRight: '10px'
  }
}));

export default useStyles;