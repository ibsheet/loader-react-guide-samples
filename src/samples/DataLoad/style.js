import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  selectStyle: {
    paddingLeft: '15px'
  },
  btnStyle: {
    height: '30px',
    background: '#4c4c57',
    color: '#fff',
    marginLeft: '10px'
  }
}));

export default useStyles;